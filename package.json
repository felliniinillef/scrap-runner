{
    "name": "scrap-runner",
    "version": "1.0.0",
    "description": "Scrap Runner Game",
    "scripts": {
        "start": "http-server -p 8080 -a 127.0.0.1",
        "dev": "live-server --port=8080 --host=127.0.0.1"
    },
    "dependencies": {
        "http-server": "^14.1.1",
        "live-server": "^1.2.2"
    }
}
